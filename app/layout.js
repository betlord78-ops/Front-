import './globals.css';
export const metadata = { title: 'SpyTON', description: 'SpyTON — Discover. Scan. Promote.' };
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
